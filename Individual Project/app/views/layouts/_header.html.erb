<nav class="navbar navbar-inverse navbar-static-top " role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">

        <% if !current_user.nil? %>

          <!-- Feed Button -->
          <li>
            <%= link_to posts_path, class: "btn btn-inverse" do %>
              <span class="glyphicon glyphicon-tasks" id="logIcon"> Feed </span>
            <% end %>
          </li>

          <!-- Users Button -->
          <li>
            <%= link_to users_path, class: "btn btn-inverse" do %>
              <span class="glyphicon glyphicon-list-alt" id="logIcon"> Users </span>
            <% end %>
          </li>

          <!-- Profile Button -->
          <li>
            <%= link_to about_path, class: "btn btn-inverse" do %>
              <span class="glyphicon glyphicon-info-sign" id="logIcon"> About </span>
            <% end %>
          </li>

          <li>
              <a class="btn btn-inverse dropdown-toggle" type="button" id="menu1" data-toggle="dropdown">
                  <span class="glyphicon glyphicon-triangle-bottom"> Account </span></a>
                  <ul class="dropdown-menu" role="menu" aria-labelledby="menu1">

                              <!-- Profile Button -->
                    <li>
                      <%= link_to current_user, class: "btn btn-inverse" do %>
                        <span class="glyphicon glyphicon-user" id="logIcon"> Profile </span>
                      <% end %>
                    </li>


                              <!-- Settings Button -->
                    <li>
                      <%= link_to edit_user_path(current_user), class: "btn btn-inverse" do %>
                        <span class="glyphicon glyphicon-wrench" id="logIcon"> Settings</span>
                      <% end %>
                    </li>
                              <!-- Log Out Button -->
                    <li>
                      <%= link_to logout_path, method: "delete", :title => "Logout", class: "btn btn-inverse" do %>
                        <span class="glyphicon glyphicon-log-out" id="logIcon"> Logout</span>
                      <% end %>
                    </li>

                  </ul>
          </li>
        
        <% else %>

        <!-- Login Page -->
          <li>
            <%= link_to "Login", root_path %>
          </li>

          <!-- Sign Up Page -->
          <li>
            <%= link_to "Sign Up", signup_path %>
          </li>

          <li><%= link_to "About", about_path %></li>

        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>